import { StyleSheet } from "react-native";

export default StyleSheet.create({
  footerContainer: {
    position: "absolute",
    bottom: -12,
    left: 0,
    right: 0,
    width: "100%",
    backgroundColor: "#fff"
  },
  footer: {
    width: "100%",
    height: 70,
    backgroundColor: "#fff",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around"
  }
})
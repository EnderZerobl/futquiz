import { StyleSheet } from "react-native";

export default StyleSheet.create({
  footer: {
    flex: 1,
    width: "100%",
    height: 70,
    backgroundColor: "#fff",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-around",
    position: "absolute",
    bottom: 0,
    left: 0
  }
})